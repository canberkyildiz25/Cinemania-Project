{
  "name": "cinemania-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "deploy": "gh-pages -d dist",
  "predeploy": "npm run build",
  "build": "vite build"
}
,
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
